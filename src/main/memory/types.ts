/**
 * Memory system types — MemoryEvent, MemoryEventType, MemoryFilter.
 *
 * These types define the structure of Noah's memory events,
 * which are used to track interactions, record emotional context,
 * and provide input for the LLM in later stages.
 */

import type { MemoryEvent, MemoryEventType, MemoryFilter } from '../../shared/types/index.js';

export type { MemoryEvent, MemoryEventType, MemoryFilter };
